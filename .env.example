# ============================================
# 智能天气助手 API v2.0 配置文件模板
# ============================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 填写真实的配置信息
# 3. .env 文件不会被 Git 追踪（已在 .gitignore 中）
# ============================================

# ----------------------------------------
# API 配置（统一使用 302.ai）
# ----------------------------------------
# 统一 API Key：同时用于对话和搜索
API_KEY=your_openai_api_key_here

# 对话 API 路径（用于 LLM 对话）
BASE_URL=https://api.302.ai/v1

# 模型配置
MODEL=gpt-4o-mini

# ----------------------------------------
# 搜索 API 配置（用于天气查询）
# ----------------------------------------
# 搜索接口路径（固定）
SEARCH_API_URL=https://api.302.ai/search1api/search

# ----------------------------------------
# 数据库配置（MySQL）
# ----------------------------------------
# 必填：用于存储天气地区信息
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456
DB_NAME=fang
DB_CHARSET=utf8mb4

# MySQL 连接配置（可选）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=fang

# ----------------------------------------
# Redis 缓存配置（可选）
# ----------------------------------------
# 如果不配置，缓存功能将自动禁用（不影响基础功能）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
# REDIS_PASSWORD=your_redis_password  # 如果 Redis 设置了密码

# ----------------------------------------
# 服务器配置（可选）
# ----------------------------------------
# API 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=6666
# DEBUG=False  # 生产环境设为 False

