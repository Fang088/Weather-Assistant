# LangChain 核心库
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20  # 新增：包含 SQL Agent 和 SQLDatabase

# 数据库
pymysql>=1.1.0
sqlalchemy>=2.0.0  # 新增：LangChain SQL 依赖 SQLAlchemy

# 配置管理
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# HTTP 请求（保留以备将来扩展）
requests>=2.31.0

# 日志（Python内置，无需安装）
# logging

# FastAPI 和服务器
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Redis 缓存（可选，不安装不影响基础功能）
redis>=5.0.0

